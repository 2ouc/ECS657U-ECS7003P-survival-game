using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using Cinemachine;
//using WSMGameStudio.RailroadSystem;

public class CameraSwitching : MonoBehaviour
{
   // private CinemachineBlendListCamera CMBlendList;

    private void Start()
    {
       // CMBlendList = GetComponent<CinemachineBlendListCamera>();
                                             //CMBlendList.Follow = FindObjectOfType<TrainController_v3>().gameObject.transform;
       // CMBlendList.Follow = FindObjectOfType<TrainController_v3>().gameObject.transform.Find("CameraPoint");
       
    }
}
